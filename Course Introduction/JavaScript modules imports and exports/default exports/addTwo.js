// In the example, we exported the function in a default export way by adding the `export default` keywords in front of the `addTwo` function in declaration.
export default function addTwo(a, b) {
  console.log(a + b);
}
